{
  "name": "wenren-home",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages:dev": "npm run build && wrangler pages dev dist",
    "test": "npm run build && node --test tests/site.test.mjs",
    "deploy": "npm test && wrangler pages deploy --branch main"
  },
  "devDependencies": {
    "vite": "8.0.13",
    "wrangler": "4.112.0"
  }
}
